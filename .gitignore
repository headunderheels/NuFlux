
# Build artifacts — never commit (2.2GB image)
*.sif

# Runtime working set (cloned NuFlux + acc-models-mc + outputs)
work/

# Common build/output detritus
*.root
output/
# Shared geometry checkout (cloned once, not ours to track)
geometry/

# Per-run directories: run.conf + outputs
runs/
